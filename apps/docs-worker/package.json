{
  "name": "pandd-docs-worker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test",
    "check": "wrangler deploy --dry-run",
    "types": "wrangler types",
    "db:local": "wrangler d1 migrations apply DOCS_DB --local"
  },
  "devDependencies": {
    "miniflare": "5.20260826.0-alpha",
    "wrangler": "4.127.0"
  }
}
