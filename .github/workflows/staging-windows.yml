name: Publish Windows staging

on:
  workflow_dispatch:
    inputs:
      version:
        description: SemVer version to publish
        required: true
        default: 1.0.0

permissions:
  contents: read

env:
  STAGING_BASE_URL: https://pub-1ada658d7c4f46b1bf109646a4a68bcb.r2.dev
  STAGING_BUCKET: pandd-launcher-staging

jobs:
  build:
    runs-on: windows-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4
      - uses: jurplel/install-qt-action@v4
        with:
          version: '6.10.2'
          arch: win64_msvc2022_64
          tools: 'tools_ifw,qt.tools.ifw.47'
          cache: true
      - name: Initialize MSVC environment
        uses: ilammy/msvc-dev-cmd@v1
      - uses: lukka/run-vcpkg@v11
      - name: Validate version
        shell: pwsh
        run: python scripts/validate_release_version.py "${{ inputs.version }}"
      - name: Configure staging launcher
        shell: pwsh
        run: >-
          cmake -S . -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DBUILD_TESTING=OFF
          -DPANDD_DISTRIBUTION_ENV=staging
          -DPANDD_DISTRIBUTION_BASE_URL=${{ env.STAGING_BASE_URL }}/
          -DPANDD_BUILD_NUMBER=${{ github.run_number }}
          -DPANDD_MANIFEST_PUBLIC_KEY_BASE64=${{ secrets.MANIFEST_PUBLIC_KEY_BASE64 }}
          -DCMAKE_TOOLCHAIN_FILE=${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake
      - name: Build and install
        shell: pwsh
        run: |
          cmake --build build --parallel
          cmake --install build --prefix "${{ github.workspace }}/staging"
      - name: Generate release metadata
        shell: pwsh
        run: |
          python scripts/generate_sbom.py --output staging/docs/pandd-game-launcher.spdx.json --version "${{ inputs.version }}" --build-metadata build/pandd-build-metadata.json --ifw-version 4.7.0
          python scripts/collect_licenses.py --output staging/licenses
      - name: Smoke test deployment
        shell: pwsh
        run: ./scripts/smoke_test_windows.ps1 -Executable 'staging/bin/PandD Game Launcher.exe'
      - name: Build unsigned online installer
        shell: pwsh
        run: >-
          python installer/build.py
          --install-tree staging
          --output ifw
          --version "${{ inputs.version }}"
          --repository-url "${{ env.STAGING_BASE_URL }}/v1/launcher/ifw/windows/x86_64"
      - name: Package and checksum unsigned artifacts
        shell: pwsh
        run: |
          $archive = 'ifw/PandD-Game-Launcher-windows-x86_64.zip'
          Compress-Archive -Path 'staging/*' -DestinationPath $archive -CompressionLevel Optimal
          python scripts/generate_checksums.py `
            'ifw/PandD-Game-Launcher-Online-Installer.exe' `
            $archive
          python scripts/generate_checksums.py --check `
            'ifw/PandD-Game-Launcher-Online-Installer.exe.sha256' `
            "$archive.sha256"
      - uses: actions/upload-artifact@v4
        with:
          name: windows-staging-${{ inputs.version }}
          path: |
            staging
            ifw

  publish:
    needs: build
    runs-on: ubuntu-24.04
    environment: staging
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install Publisher dependencies
        run: python -m pip install --requirement publisher/requirements.txt
      - uses: actions/download-artifact@v4
        with:
          name: windows-staging-${{ inputs.version }}
          path: artifact
      - name: Assemble public staging tree
        env:
          STAGING_VERSION: ${{ inputs.version }}
        run: |
          mkdir -p public/v1/launcher/ifw/windows/x86_64
          cp -a artifact/ifw/repository/. public/v1/launcher/ifw/windows/x86_64/
          destination="public/v1/launcher/installers/windows/x86_64/$STAGING_VERSION"
          mkdir -p "$destination"
          cp artifact/ifw/PandD-Game-Launcher-Online-Installer.exe* "$destination/"
          cp artifact/ifw/PandD-Game-Launcher-windows-x86_64.zip* "$destination/"
          python publisher/publisher.py publish-announcements --source backend/content/announcements --output public
          for locale in ja-JP en-US; do
            python publisher/publisher.py publish-changelog --source "backend/content/launcher/changelog.$locale.json" --output public --locale "$locale"
            python publisher/publisher.py publish-launcher --source "backend/content/launcher/release.$locale.json" --output public --base-url "$STAGING_BASE_URL" --locale "$locale" --platform windows --arch x86_64
          done
      - name: Upload immutable objects then mutable pointers
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
        run: |
          aws --version
          python publisher/publisher.py upload --output public --endpoint "$R2_ENDPOINT" --bucket "$STAGING_BUCKET"
